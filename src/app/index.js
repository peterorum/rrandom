var React = require('react');

var ReactDOM = require('react-dom');

var RandomApp  = require('./components/random-app/random-app');

ReactDOM.render(
  <RandomApp />,
  document.getElementById('random-app')
);
